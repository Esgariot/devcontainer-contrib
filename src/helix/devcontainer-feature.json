{
	"name": "helix",
	"id": "helix",
	"version": "1.0.0",
	"description": "helix (via Github Releases)",
	"options": {
		"version": {
			"default": "latest",
			"description": "Select the version to install.",
			"proposals": [
				"latest"
			],
			"type": "string"
		}
	},
	"installsAfter": [
		"ghcr.io/devcontainers-contrib/features/gh-release"
	]
}